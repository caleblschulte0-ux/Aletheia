{
  "revision": 30,
  "providers": [
    {
      "id": "aletheia.local",
      "description": "Aletheia's own Python modules, run locally or in CI",
      "kind": "local_module"
    },
    {
      "id": "github.actions",
      "description": "GitHub Actions in this repo executing on schedule or push",
      "kind": "github_actions"
    },
    {
      "id": "github.api",
      "description": "GitHub REST API via aletheia.gh (FLEET_TOKEN / GITHUB_TOKEN)",
      "kind": "api",
      "authentication": "FLEET_TOKEN or GITHUB_TOKEN"
    },
    {
      "id": "chatgpt.app",
      "description": "ChatGPT app (subscription): voice/text interface + GitHub connector writes",
      "kind": "subscription_client",
      "authentication": "operator's ChatGPT subscription"
    },
    {
      "id": "claude.session",
      "description": "Claude Code sessions (subscription): builds and maintains Aletheia and the fleet",
      "kind": "subscription_client",
      "authentication": "operator's Claude subscription"
    },
    {
      "id": "claude.cli",
      "description": "Claude CLI installed on the operator's PC, signed in to his own subscription; run with --tools \"\" so it can emit text and nothing else",
      "kind": "subscription_client",
      "authentication": "the operator's Claude subscription via the CLI's own login; no API key",
      "notes": "Reached only through aletheia.reasoner, which bounds it: no tools, one turn, no session persistence, a timeout, a neutral working directory, and brain.validate_output on everything it returns."
    },
    {
      "id": "home_assistant",
      "description": "The operator's own Home Assistant server on his own network, reached over its documented REST API",
      "kind": "api",
      "authentication": "ALETHEIA_HASS_URL + ALETHEIA_HASS_TOKEN in the environment; never committed",
      "notes": "One hub instead of twelve vendor clouds (§152). Reading entity state is ungated; moving a device needs an APPROVED approval and re-reads the kill switch before each one."
    }
  ],
  "agents": [
    {
      "id": "claude",
      "provider": "claude.session",
      "description": "Primary coding/review worker; builds and maintains Aletheia when authorized",
      "roles": [
        "CODING",
        "CODE_REVIEW",
        "DEEP_REASONING",
        "PLANNING"
      ],
      "strengths": "implementation, repo surgery, tests",
      "limitations": "live PC/hardware work requires the operator's machine; wake-up remains provider/session dependent"
    },
    {
      "id": "chatgpt",
      "provider": "chatgpt.app",
      "description": "Thea voice/advisor and an authorized coding/review worker for explicitly operator-assigned work",
      "roles": [
        "VOICE",
        "GENERAL_REASONING",
        "RESEARCH",
        "WRITING",
        "CODE_REVIEW",
        "CODING",
        "PLANNING"
      ],
      "strengths": "connected tools, research, review, isolated draft implementation",
      "limitations": "non-Claude code requires Claude line-by-line review before merge; no direct Windows PC access in this environment"
    }
  ],
  "capabilities": [
    {
      "id": "fleet.read",
      "description": "Resolve the fleet registry: repos, roles, agents, watch lists, front-door grants",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.pulse, aletheia.act, aletheia.intercom, CLI",
      "module": "aletheia.fleet"
    },
    {
      "id": "pulse.collect",
      "description": "Observe every fleet repo: commits, workflows, state files, vitals, transitions, alerts",
      "provider": "github.api",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "pulse.yml (6-hourly) and CLI",
      "verification": "pulse names every registry repo or records why not",
      "notes": "verified live on main 2026-08-25: reads commits, workflow outcomes, state files and vitals for every PUBLIC fleet repo using the default GITHUB_TOKEN. Only private repos need the FLEET_TOKEN secret â€” today that is etsy_maker alone, which the pulse records honestly as a 404 rather than omitting.",
      "module": "aletheia.pulse"
    },
    {
      "id": "brief.compose",
      "description": "Compose the morning digest with day-over-day deltas and deliver via rolling issue",
      "provider": "github.actions",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "brief.yml (daily) and CLI",
      "module": "aletheia.brief"
    },
    {
      "id": "sentinel.alert",
      "description": "Notice health transitions; open/comment/close the rolling fleet-alert issue",
      "provider": "github.actions",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "pulse.yml after each pulse",
      "module": "aletheia.sentinel"
    },
    {
      "id": "journal.append",
      "description": "Durable append-only memory: decisions, events, actions, notes",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "every capability that acts; CLI for operator notes",
      "module": "aletheia.journal"
    },
    {
      "id": "journal.search",
      "description": "Search the memory by term, kind, or recency",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "CLI; brief.compose (last-24h section)",
      "module": "aletheia.journal"
    },
    {
      "id": "goal.plan",
      "description": "Goals as tracked data: open plans, steps with states, journaled lifecycle",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "CLI, intercom plan_* commands; surfaced by pulse/brief/wall",
      "module": "aletheia.plans"
    },
    {
      "id": "task.persist",
      "description": "Durable task engine: lifecycle, dependencies, retries surviving restarts",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "CLI (aletheia.tasks); intercom wiring is the next milestone",
      "verification": "state/tasks/*.json round-trips through contracts.validate_task",
      "module": "aletheia.tasks"
    },
    {
      "id": "github.workflow.dispatch",
      "description": "Run a fleet repo's workflow through its front door",
      "provider": "github.api",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "registry_grant",
      "caller": "aletheia.act CLI and intercom 'dispatch' commands",
      "verification": "next pulse shows the run",
      "notes": "gated by front_door in config/fleet.json â€” default grants only Aletheia's own workflows"
    },
    {
      "id": "github.issue.create",
      "description": "File an issue on a fleet repo",
      "provider": "github.api",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "registry_grant",
      "caller": "aletheia.act CLI and intercom 'issue' commands"
    },
    {
      "id": "suggestion.rule",
      "description": "Record a ruling on a ChatGPT suggestion (doing/not_doing/later/in_progress/done)",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "CLI; intercom 'rule' commands",
      "module": "aletheia.suggestions"
    },
    {
      "id": "intercom.relay",
      "description": "Operator speaks to ChatGPT (voice/text, no API keys); commands relayed as JSON, executed through registry gates, receipts written back",
      "provider": "chatgpt.app",
      "status": "NEEDS_CONFIGURATION",
      "risk_class": "medium",
      "approval_policy": "registry_grant",
      "caller": "intercom.yml on exchange/commands pushes (cloud kinds) + the Core's sync loop on the PC (LOCAL_KINDS: browse_read, browse_shot) â€” static partition, one executor per command",
      "verification": "every command gets a committed .result.json receipt",
      "notes": "executor built and tested (15 command kinds incl. halt/resume/approve/deny/remember/tasks); needs the operator to create the ChatGPT Project per exchange/INTERCOM.md",
      "module": "aletheia.intercom"
    },
    {
      "id": "agent.delegate",
      "description": "Hand a durable task to an AI worker (Claude builds; ChatGPT reviews/authors) and track it",
      "provider": "github.actions",
      "status": "EXPERIMENTAL",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "director in pulse.yml: files approval-gated work orders for READY assigned tasks",
      "notes": "dispatch half is built+tested; EXPERIMENTAL until the first live round-trip (worker completes a work order with evidence). Worker wake-up is still each app's own scheduled tasks.",
      "module": "aletheia.director"
    },
    {
      "id": "computer.control",
      "description": "Operate the Windows PC: observe, open apps, click, type (accessibility-first)",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "aletheia.computer.execute â€” reachable through the local Core's POST /api/computer and the CLI; every run needs an approval bound to that exact plan",
      "module": "aletheia.computer",
      "verification": "run computer-6c7d4dd6357b, 2026-08-26T18:57Z: approval phase7-notepad-live-2 APPROVED by the operator in the Command Center; open_app/wait/focus/set_text all verified, typed text read back through UIA and matched exactly; 33 hermetic tests + tests/test_core_computer.py",
      "notes": "Live-verified on the operator's Windows 11 PC 2026-08-26. Two rounds of real acceptance found and fixed: PowerShell mangling the harness's embedded Python; set_edit_text absent on Win11's Document control (UIA Value pattern fallback with read-back verification); a window match that could reach operator documents (plans now scope to their own scratch file). Approvals remain operator_always, sha256-bound to the exact plan, consumed once even on failure."
    },
    {
      "id": "phone.call",
      "description": "Place and hold a phone conversation on the operator's behalf (identifies itself as Aletheia)",
      "provider": "aletheia.local",
      "status": "EXPERIMENTAL",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.phone_cli ready|prepare|dial|observe|end - the front door aletheia.phone_v0 never had",
      "notes": "Phase 12. aletheia/phone_v0.py had been a complete session controller with NO caller anywhere in the repo, and one declared fake transport; that is most of why the phase stalled. aletheia/phone_windows.py is the real transport: Phone Link against the operator's paired iPhone. Dialling is a tel: URI aimed at Phone Link's own AUMID rather than the protocol default - on this machine the classic tel: handler is Skype for Business and three packaged apps claim it, so a naive dial opens the wrong app and places no call. A call counts as CONNECTED only when the Hands-Free endpoints are ACTIVE in both directions, which is a fact from MMDevice rather than a label read off a button. DTMF and hangup are explicitly NOT implemented and say so, because an IVR plan that 'succeeded' having pressed nothing would be worse than a refusal. §19's identity disclosure is unchanged and still enforced in calls.py before any dial.",
      "verification": "Machine readiness verified live 2026-08-27 (`phone_cli ready` -> True: Phone Link installed and running, iPhone paired, 30 audio devices, call audio correctly reported NOT live with no call up). 24 hermetic tests. EXPERIMENTAL because NO CALL HAS BEEN PLACED: the first supervised test call to a safe number is what promotes this, and placing it is the operator's to authorize.",
      "module": "aletheia.phone_windows"
    },
    {
      "id": "voice.wake",
      "description": "Room voice: local wake word ('Thea'), spoken requests into the same Core",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "Windows task 'AletheiaVoice' runs python -m aletheia.voice_room at logon: vosk offline recognizer (no API keys) + grammar-constrained wake spotter -> POST /api/voice -> the same intercom gates as every channel -> SAPI voice reply",
      "module": "aletheia.voice_room",
      "notes": "Live-proven 2026-08-26 by machine self-test: TTS spoke 'Thea, what's going on?' through the speakers, the wake spotter fired, the Core answered through the gates, and she replied out loud ('2 fleet alerts. 7 live tasks.'). Found and fixed on the way: the mic endpoint was hardware-MUTED (the wall mic's silent failure too), the array runs ~500/32768 peak (software AGC added), and the small model cannot spell 'Thea' in open dictation (grammar-constrained spotter added). Audio never leaves the machine; unaddressed speech is dropped unjournaled.",
      "verification": "self-test round trip on the operator's PC + the task's own listener heard the room independently (journal voice:room 2026-08-26T22:37Z) + 9 hermetic loop tests"
    },
    {
      "id": "email.read",
      "description": "Check the inbox: unread senders + subjects, spoken back ('Thea, check my email')",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.mail.check_unread via intercom kind email_check (LOCAL â€” runs on the PC) and voice; runtime poll each Core beat (>=300s apart) turns new unread into private bus events and exact-match reply correlation — ambiguity refused, never guessed",
      "verification": "live on the operator's PC 2026-08-26: IMAP login as openrangeinteractive@gmail.com succeeded, 185 unread listed via mail.check_unread; credentials in ~/.aletheia/mail.json (never the repo)",
      "notes": "Configured 2026-08-26 with an operator-supplied Gmail app password (revocable at myaccount.google.com/apppasswords).",
      "module": "aletheia.mail"
    },
    {
      "id": "email.send",
      "description": "Draft and send email with approval gate",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "aletheia.mail.send_approved â€” the Core delivers every APPROVED draft each sync tick; drafts come from voice ('Thea, email X that ...'), the intercom kind email_draft, or the Command Center",
      "verification": "live end to end on the operator's PC 2026-08-26: draft mail-a1e1957d0f -> operator_always approval decided with the operator's own word ('Approved', quoted in the decision) -> SMTP delivery -> exactly-once .sent receipt -> the message verified PRESENT in the inbox by independent IMAP read. Plus tests/test_mail.py (15) on the content-bound approval discipline.",
      "notes": "Credentials in ~/.aletheia/mail.json (app password, revocable). Every send remains operator_always, bound to the sha256 of the exact draft.",
      "module": "aletheia.mail"
    },
    {
      "id": "calendar.read",
      "description": "Read availability, reason about work hours and buffers",
      "provider": "aletheia.local",
      "status": "NEEDS_CONFIGURATION",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.runtime.tick each Core beat (_refresh_calendar); python -m aletheia.calendar_live_cli status|sync; python -m aletheia.calendar_auth google|microsoft",
      "notes": "aletheia/calendar_live.py picks ONE source of truth - an official OAuth provider when configured, the secret-ICS fallback otherwise - because mirroring both against the same account would double-count busy time. Consent is a one-time local desktop flow (aletheia/calendar_auth.py): system browser, PKCE-S256, high-entropy state compared in constant time, one loopback callback on an ephemeral port. No code, access token, refresh token or client secret is printed, journaled or committed; the refresh config lives in ~/.aletheia/calendar-live.json and rotated tokens in gitignored state/private/calendar/oauth. ChatGPT PR (calendar-providers-v1), Claude-reviewed 2026-08-27. Still NEEDS_CONFIGURATION and not AVAILABLE: the code is real and tested, but the operator has authorized no account, so there is nothing to read. Reading it live is what promotes it.",
      "verification": "tests/test_calendar_live_providers.py + test_calendar_auth.py + test_ics_busy_semantics.py; TRANSP:TRANSPARENT and STATUS:CANCELLED no longer block time, which was a wrong answer to 'am I free at 3?'",
      "module": "aletheia.calendar_live"
    },
    {
      "id": "room.scene",
      "description": "Unified device layer (Home Assistant or equivalent): lights, scenes, media",
      "provider": "aletheia.local",
      "status": "NEEDS_CONFIGURATION",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "python -m aletheia.hass observe|scene; aletheia.runtime.tick refreshes device reachability each beat when a hub is configured",
      "notes": "The provider room.plan() has always ended by waiting for (READY_FOR_PROVIDER) now exists: Home Assistant's REST API. Needs ALETHEIA_HASS_URL and a long-lived ALETHEIA_HASS_TOKEN in the environment - the code is real, the token is the operator's to create. A scene needs an APPROVED approval, refuses any device not observed ONLINE, and re-reads the kill switch between devices.",
      "module": "aletheia.hass",
      "verification": "tests/test_reach.py: no approval means no HTTP call at all; an offline device is refused rather than silently skipped"
    },
    {
      "id": "policy.halt",
      "description": "The kill switch: halt suspends every acting capability until an operator resume",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "intercom halt/resume commands, CLI; enforced in act + intercom + director",
      "verification": "state/policy/halt.json; halted receipts"
    },
    {
      "id": "policy.approve",
      "description": "Durable approval objects: requested as issues or by workers, decided by owner comment or by voice",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "director (delegate gate), approvals.yml on owner comments, intercom approve/deny, CLI"
    },
    {
      "id": "orchestrator.compile",
      "description": "Deterministic goal-to-tasks compiler with evidence-gated completion (a step is done only when its task completed WITH evidence)",
      "provider": "github.actions",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "pulse.yml every run; CLI"
    },
    {
      "id": "memory.remember",
      "description": "Structured memory with provenance (identity/preferences/people/organizations); corrections overwrite but record what they replaced",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "intercom remember commands, CLI, Claude sessions"
    },
    {
      "id": "memory.recall",
      "description": "Recall a memory and answer 'why do you think that' from provenance",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "CLI; ChatGPT reads memory/ raw for reference resolution"
    },
    {
      "id": "core.serve",
      "description": "The local Core V0: persistent service on the operator's PC â€” internal API (/api/*), the wall at /, Command Center at /command.html; loopback-only until authentication exists",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "none",
      "caller": "operator runs `python -m aletheia.core` on the Windows PC; Command Center + wall are its clients",
      "verification": "GET /api/status; 7 tests in test_core.py incl. live HTTP round-trips",
      "notes": "Live on the operator's Windows PC since 2026-08-26 and self-evidencing: the Core's own checkpoint commits land in this repo (193 in one day), and on 2026-08-27T04:38:46Z it pulled merge 095bbfde, journaled 'code updated (12 files) — restarting to run it', and the supervisor relaunched it onto the new code — back serving 6 seconds later. (Registry said NEEDS_CONFIGURATION until 2026-08-27; that was stale — the blocker it named, the operator starting it, was cleared the day before.)",
      "module": "aletheia.core"
    },
    {
      "id": "core.sync",
      "description": "The Core keeps the repo fresh: pull commands, execute PC-only kinds, push receipts + journal",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "aletheia.core.start_sync_loop â€” on by default when `python -m aletheia.core` runs inside a cloned repo; degrades honestly (loop OFF, /api/sync says why) outside one",
      "verification": "tests/test_sync.py + tests/test_core_sync.py drive the full round trip against a real local bare repo, including push races, rebase-conflict abort, receipt idempotency, and a dead remote",
      "notes": "Live on the operator's Windows PC since 2026-08-26 and self-evidencing: the Core's own checkpoint commits land in this repo (193 in one day), and on 2026-08-27T04:38:46Z it pulled merge 095bbfde, journaled 'code updated (12 files) — restarting to run it', and the supervisor relaunched it onto the new code — back serving 6 seconds later. (Registry said NEEDS_CONFIGURATION until 2026-08-27; that was stale — the blocker it named, the operator starting it, was cleared the day before.)",
      "module": "aletheia.sync"
    },
    {
      "id": "core.supervise",
      "description": "Aletheia stays on: relaunch the Core on crash, restart it onto newly merged code, start at Windows logon",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "start-aletheia.bat and bootstrap.ps1 run `python -m aletheia.supervisor`; `supervisor install` registers the hidden at-logon Scheduled Task",
      "verification": "tests/test_supervisor.py (backoff doubles and caps, self-update restarts immediately and resets backoff, Ctrl+C stops, install refuses honestly off Windows) + tests/test_core_sync.py SelfUpdateCase (code commit fires restart against a real repo, state-only commit does not)",
      "notes": "Live on the operator's Windows PC since 2026-08-26 and self-evidencing: the Core's own checkpoint commits land in this repo (193 in one day), and on 2026-08-27T04:38:46Z it pulled merge 095bbfde, journaled 'code updated (12 files) — restarting to run it', and the supervisor relaunched it onto the new code — back serving 6 seconds later. (Registry said NEEDS_CONFIGURATION until 2026-08-27; that was stale — the blocker it named, the operator starting it, was cleared the day before.)",
      "module": "aletheia.supervisor"
    },
    {
      "id": "voice.wall",
      "description": "Talk to the page itself: the wall/Command Center hear 'Thea, ...' and speak the answer â€” no side app",
      "provider": "aletheia.local",
      "status": "NEEDS_CONFIGURATION",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "interface/voice.js (Web Speech API in the operator's browser) -> POST /api/voice -> voice.interpret -> the same run_command gates as every channel",
      "verification": "tests/test_voice.py: 15 tests â€” wake word, halt/resume, spoken URLs ('example dot com'), approve refuses to guess between two pending, unrecognized speech becomes a journaled note (never a guessed action), endpoint round trip on a live Core",
      "notes": "NEEDS_CONFIGURATION until the Core runs on the PC and the operator clicks the mic indicator once (browser permission). Ears/mouth are the browser's own (Chrome/Edge); the interpreter is deterministic on purpose - unmatched speech is journaled and said so, never guessed (Â§104). ChatGPT Voice remains the away-from-home channel. 2026-08-26: the PC's mic endpoint was found MUTED in Windows — the likely cause of the wall mic's silent failure; unmuted, and voice_room --check now reports mute honestly.",
      "module": "aletheia.voice"
    },
    {
      "id": "browser.read",
      "description": "Observe any web page through the authorized browser profile: title, visible text, links, screenshots",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.browse CLI (read/shot); intercom kinds browse_read/browse_shot via the Core's sync loop; the Command Center's /api/command",
      "verification": "12 hermetic tests in tests/test_browse.py drive a real Chromium against a loopback fixture site",
      "notes": "requires the optional playwright dependency; browse.available() reports honestly and callers degrade rather than crash",
      "module": "aletheia.browse"
    },
    {
      "id": "browser.interact",
      "description": "Act on a web page â€” click, type, select, submit â€” inside the operator's own authenticated session",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "aletheia.browse.interact â€” refuses without an APPROVED approval, before the browser opens",
      "verification": "tested end to end: an approved run fills a form, selects an option and submits it; unapproved, denied, malformed and halted runs are all refused",
      "notes": "never bypasses authentication: the operator signs in once via 'python -m aletheia.browse login <url>' into a dedicated persistent profile (cache/browser-profile, gitignored). Permission to read a page is never permission to press its buttons (Â§61).",
      "module": "aletheia.browse"
    },
    {
      "id": "event.emit",
      "description": "Durable local event bus: immutable one-file-per-event records under private state",
      "module": "aletheia.events",
      "provider": "aletheia.local",
      "risk_class": "low",
      "approval_policy": "none",
      "status": "AVAILABLE",
      "caller": "aletheia.core.core_tick emits core.sync_failed/core.sync_recovered on pull transitions; python -m aletheia.events emit; consumed by aletheia.runtime.process_new_events; producers: aletheia.mail.poll_events (mail.received/mail.reply, throttled, fingerprints only) and runtime.mirror_pulse_events (fleet.health_changed)",
      "notes": "Private by default (state/private/): event subjects/summaries carry personal facts and the repo is public. Sensitive attribute keys refused; 32KiB cap; occurred_at must be a real aware timestamp.",
      "verification": "tests/test_events.py (13) + tests/test_runtime_expansion.py::TestEventConsumption"
    },
    {
      "id": "event.watch",
      "description": "Durable watchers: 'tell me when X' matched against every bus event, triggering exactly once (or persistently), surfacing as notifications",
      "module": "aletheia.events",
      "provider": "aletheia.local",
      "risk_class": "low",
      "approval_policy": "none",
      "status": "AVAILABLE",
      "caller": "python -m aletheia.events watch/cancel/watchers; evaluated at emit time and consumed by aletheia.runtime.process_new_events -> notification.center",
      "notes": "Immutable definitions + append-only cancellation/trigger receipts; a corrupt watcher is isolated, never fatal to the bus. At least one narrowing match required; no regex/code in matchers.",
      "verification": "tests/test_events.py + tests/test_runtime_expansion.py::test_watcher_trigger_becomes_notification_once"
    },
    {
      "id": "policy.delegate",
      "description": "Bounded standing authority grants with zero defaults",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "python -m aletheia.assistant grant new|list|revoke; consumed by aletheia.authority.satisfy from aletheia.policy.request",
      "verification": "tests/test_integration_primitives.py::TestAuthority + tests/test_assistant.py grant verbs",
      "module": "aletheia.authority",
      "notes": "Grants are now consumed: aletheia.policy.request(capability=...) spends an eligible grant and records a claim receipt. Was EXPERIMENTAL because no acting path consumed one - that was the gap, and it is closed. What a grant can cover is unchanged and still refuses high-risk and operator_always."
    },
    {
      "id": "action.verify",
      "description": "Action records with plan hashes, attempts and evidence-backed verification",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant outcome start/attempt/evidence/verify (workers record evidence-gated action records); aletheia.verification profiles + runtime receipt reconciliation each Core beat (mail receipts, computer/browser journal, delegated tasks -> private ActionRecords)",
      "module": "aletheia.outcomes"
    },
    {
      "id": "automation.define",
      "description": "Define timezone-aware one-time/interval/daily/weekly schedules privately",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant schedule add-once/add-interval/add-daily/add-weekly and python -m aletheia.scheduler list/due",
      "verification": "exclusive occurrence receipts prevent duplicate claims",
      "module": "aletheia.scheduler"
    },
    {
      "id": "automation.execute",
      "description": "Execute due private schedules through the existing command grammar and gates",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "none",
      "caller": "aletheia.core.core_tick -> aletheia.runtime.run_due_schedules every sync beat; due commands revalidate through intercom.validate_kind_args and execute through intercom.execute_command with the same gates as voice",
      "notes": "Wired into core_tick 2026-08-26 (Claude review of PR30 — its draft claimed the wiring existed; it did not, so it was built). Exactly-once via occurrence receipts; halt respected; failures notify the operator.",
      "module": "aletheia.runtime",
      "verification": "tests/test_core_sync.py::test_runtime_rides_the_sync_beat — a due schedule executes through the real intercom inside core_tick, exactly once; live /api/runtime check on the operator's PC 2026-08-26"
    },
    {
      "id": "contacts.resolve",
      "description": "Private exact/alias contact resolution with ambiguity refusal",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.contacts and aletheia.meetings.propose",
      "module": "aletheia.contacts"
    },
    {
      "id": "calendar.availability",
      "description": "Timezone-aware local conflict, buffer, work-window and multi-day slot reasoning",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.calendar, aletheia.meetings.propose, python -m aletheia.assistant meet",
      "module": "aletheia.calendar"
    },
    {
      "id": "meeting.plan",
      "description": "Resolve a person and propose constrained meeting slots without contacting or booking",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant meet -> aletheia.meetings.propose",
      "module": "aletheia.meetings"
    },
    {
      "id": "communication.track",
      "description": "Private channel-neutral conversations, messages, expected replies and deadlines",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant comm thread/message/expect; aletheia.runtime.evaluate_replies each Core sync beat",
      "module": "aletheia.communications"
    },
    {
      "id": "notification.center",
      "description": "Private deduped operator notifications with read/ack state",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "aletheia.runtime (reply transitions, watcher triggers, proactive proposals, schedule failures), Core GET /api/notifications + POST /api/notifications/ack, Command Center panel, python -m aletheia.assistant notifications/ack",
      "module": "aletheia.notifications"
    },
    {
      "id": "project.manage",
      "description": "Private first-class projects linking goals, tasks, people, blockers and decisions",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant project new/update/list; current_state.snapshot reads them",
      "module": "aletheia.projects"
    },
    {
      "id": "context.resolve",
      "description": "Bounded explicit recent referents; ambiguous context is refused",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant context remember/resolve",
      "module": "aletheia.context"
    },
    {
      "id": "current_state.read",
      "description": "Canonical NOW aggregation across tasks, projects, approvals, replies, notices and schedules",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "Core GET /api/state and python -m aletheia.assistant state",
      "module": "aletheia.current_state"
    },
    {
      "id": "recovery.plan",
      "description": "Classify failures and plan bounded retry/wait/escalation without executing retries",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant recover",
      "module": "aletheia.recovery"
    },
    {
      "id": "capability.gap.assess",
      "description": "Compare required capabilities to registry truth",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.runtime.reconcile_task_gaps each Core sync beat; aletheia.handler; aletheia.composition",
      "module": "aletheia.gaps"
    },
    {
      "id": "capability.gap.materialize",
      "description": "Turn missing/configuration/verification gaps into durable work",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "aletheia.runtime.reconcile_task_gaps (gap tasks created, originals paused/resumed); aletheia.handler.create/refresh",
      "verification": "configuration gaps become operator tasks; build gaps become worker tasks",
      "module": "aletheia.gaps"
    },
    {
      "id": "handle.plan",
      "description": "Persist a resolved request, capability requirements and resume state",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant handle new/refresh/done/cancel",
      "module": "aletheia.handler",
      "notes": "v2 (2026-08-26): bounded state machine — candidate paths with capability-based selection, WAITING_EXTERNAL, recovery-scheduled retries, AWAITING_VERIFICATION, evidence-gated completion; runtime reconciles/resumes each Core beat. A fallback can change HOW a goal is attempted, never what is permitted."
    },
    {
      "id": "proactive.evaluate",
      "description": "Evaluate bounded event rules into proposals with cooldown/dedupe",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "aletheia.runtime.process_new_events each Core sync beat evaluates every rule against new bus events; rules from python -m aletheia.assistant rule new",
      "verification": "rules never directly execute actions",
      "module": "aletheia.proactive"
    },
    {
      "id": "capability.compose",
      "description": "Compose named outcomes from reusable capability requirements without authority escalation",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant compose plan",
      "module": "aletheia.composition"
    },
    {
      "id": "brain.contract",
      "description": "Model-independent bounded reasoning-provider output contract",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant interpret (deterministic FALLBACK); provider adapters must pass validate_output before any proposal reaches planners",
      "verification": "unknown output fields and malformed proposals are refused",
      "module": "aletheia.brain"
    },
    {
      "id": "device.registry",
      "description": "Private provider-neutral room/device identity, abilities and observed online state",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant device register/room; room scene planning",
      "module": "aletheia.devices"
    },
    {
      "id": "room.scene.plan",
      "description": "Validate a room scene against verified-online devices and declared abilities",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant scene new/plan",
      "module": "aletheia.room"
    },
    {
      "id": "place.manage",
      "description": "Private saved places and provenance-backed travel-time observations",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant place new/resolve/travel",
      "module": "aletheia.places"
    },
    {
      "id": "document.intelligence",
      "description": "Private bounded text ingestion, hashing and literal evidence search",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant doc ingest/search",
      "module": "aletheia.documents"
    },
    {
      "id": "shopping.plan",
      "description": "Private requirements/candidates/selection workflow ending in an approval-bounded purchase proposal",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant shop new/candidate/select/propose",
      "module": "aletheia.shopping"
    },
    {
      "id": "purchase.execute",
      "description": "Execute an actual purchase with money",
      "provider": "aletheia.local",
      "status": "EXPERIMENTAL",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.errands new --kind purchase; aletheia.runtime.tick runs authorized errands each Core beat",
      "notes": "aletheia.errands: one approval-bound, evidence-verified web errand executed through the existing browser gate (browse.interact refuses before the browser opens unless the approval is APPROVED). Authorization binds to a sha256 of {site, kind, steps, ceiling}; a step or a ceiling edited afterwards is refused, not adapted. §143 boundaries - bank step-up, one-time codes, CAPTCHA, signatures, ID checks, biometrics, consent - stop the errand and hand the operator the remainder. A purchase additionally declares a spending ceiling, and the LARGEST money figure observed on the page is checked against it before anything is clicked. EXPERIMENTAL until the first real merchant checkout completes with a receipt: 22 hermetic tests drive it, no live purchase has been made.",
      "module": "aletheia.errands",
      "verification": "tests/test_errands.py: over-ceiling pages abandoned before the click, tampered plans refused, every §143 boundary class recognised"
    },
    {
      "id": "subscription.track",
      "description": "Private recurring charge/renewal visibility and cancellation proposals",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant subs new/list/cancel-request",
      "module": "aletheia.subscriptions"
    },
    {
      "id": "subscription.cancel",
      "description": "Cancel a live external subscription",
      "provider": "aletheia.local",
      "status": "EXPERIMENTAL",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.errands new --kind cancellation; aletheia.runtime.tick runs authorized errands each Core beat",
      "module": "aletheia.errands",
      "notes": "aletheia.errands: one approval-bound, evidence-verified web errand executed through the existing browser gate (browse.interact refuses before the browser opens unless the approval is APPROVED). Authorization binds to a sha256 of {site, kind, steps, ceiling}; a step or a ceiling edited afterwards is refused, not adapted. §143 boundaries - bank step-up, one-time codes, CAPTCHA, signatures, ID checks, biometrics, consent - stop the errand and hand the operator the remainder. A cancellation spends nothing and is refused if it declares a ceiling. EXPERIMENTAL until a live cancellation round-trips with the merchant's confirmation."
    },
    {
      "id": "finance.visibility",
      "description": "Private read-only balances/transactions snapshots and net-worth aggregation",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant finance account/accounts/net",
      "module": "aletheia.finance"
    },
    {
      "id": "finance.transact",
      "description": "Move money, pay bills or trade assets",
      "provider": "aletheia.local",
      "status": "NOT_BUILT",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "ticket — Phase 22 financial action adapters require live provider, approval and verification design",
      "notes": "Deliberately NOT_BUILT, and this is a decision rather than a backlog item. Bank transfers, bill payments and trades are identity-checked, step-up authenticated and closed to automation by their own terms - §143's list almost word for word. There the boundary IS the mechanism, so driving it would defeat a control that exists for the operator's benefit. What IS built is the other half of §143: finance.hand_off prepares the whole movement - account, payee, amount, due date, why he asked - and stops, so his remaining work is approving something he can see in full. finance.settle records the reference he gets back (§30)."
    },
    {
      "id": "vehicle.manage",
      "description": "Private vehicle facts, odometer and maintenance due calculations",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant vehicle new/odometer/rule/due",
      "module": "aletheia.vehicles"
    },
    {
      "id": "travel.plan",
      "description": "Private itinerary with observed booking facts and missing-component checks",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant trip new/item/gaps",
      "module": "aletheia.travel"
    },
    {
      "id": "reservation.plan",
      "description": "Private reservation candidate/selection workflow with confirmation truth",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant reserve new/candidate/select/propose/confirm",
      "module": "aletheia.reservations"
    },
    {
      "id": "reservation.book",
      "description": "Book or cancel a live external reservation",
      "provider": "aletheia.local",
      "status": "EXPERIMENTAL",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.errands new --kind reservation; aletheia.runtime.tick runs authorized errands each Core beat",
      "module": "aletheia.errands",
      "notes": "aletheia.errands: one approval-bound, evidence-verified web errand executed through the existing browser gate (browse.interact refuses before the browser opens unless the approval is APPROVED). Authorization binds to a sha256 of {site, kind, steps, ceiling}; a step or a ceiling edited afterwards is refused, not adapted. §143 boundaries - bank step-up, one-time codes, CAPTCHA, signatures, ID checks, biometrics, consent - stop the errand and hand the operator the remainder. reservations.confirm() still requires a real confirmation identifier from the provider before a booking is CONFIRMED - completing the errand is not the same as being booked (§30). EXPERIMENTAL until a live booking round-trips."
    },
    {
      "id": "calendar.write",
      "description": "Change a live external calendar (create/update/cancel) through a provider adapter",
      "module": "aletheia.calendar_live",
      "provider": "aletheia.local",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "status": "EXPERIMENTAL",
      "caller": "aletheia.calendar_provider.execute_write_plan behind a hash-bound operator approval; python -m aletheia.calendar_live_cli execute",
      "notes": "Google Calendar and Microsoft Graph create/update/delete are now really implemented (previously no provider existed at all). Still EXPERIMENTAL and still operator_always: no write has ever round-tripped against a live account, and a calendar write changes something other people can see. The write scope is requested only when the operator passes --enable-writes at consent time, so a read-only authorization cannot later be used to write.",
      "verification": "tests/test_calendar_provider.py: approval binding, hash mismatch refusal, provider-verification failure raises, conflict on locally-dirty events"
    },
    {
      "id": "attention.policy",
      "description": "When may a notice interrupt Caleb: quiet hours, priority escalation with age, bypass list — classification only, never delivery",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "runtime.tick runs attention.reconcile LAST every Core beat; python -m aletheia.proactive_cli and python -m aletheia.attention configure/status/ready",
      "verification": "tests/test_attention.py + tests/test_attention_runtime.py (14 tests): quiet hours across midnight, escalation chains strictly increase, mark_delivered revalidates at evidence time so a stale READY can never be recorded as a legitimate delivery, acknowledged notices cancel",
      "notes": "Zero-intrusion defaults (quiet hours off, no bypass, no escalation) until the operator configures it. Reviewed ChatGPT branch proactive-attention-v1; repair applied in review: this registry entry — the branch shipped the capability live in every tick without registering it (§104).",
      "module": "aletheia.attention"
    },
    {
      "id": "audio.route",
      "description": "Phase 11: logical audio routing plans (mic/speaker/virtual cable) — approval-bound, verified by evidence, no raw audio handled",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "python -m aletheia.audio_cli; aletheia.phone_cli selects it for Phase 12; aletheia.audio_router.activate/verify_active/stop",
      "verification": "LIVE on the operator's PC 2026-08-27: plan built, activation REFUSED while the approval was PENDING, then approved and activated mic -> headphone jack; verified 41,600 frames in / 40,960 out across two observations; stopped cleanly. Plus 22 hermetic tests with an injected sounddevice.",
      "notes": "The control plane shipped with only InMemoryAudioBackend, a declared fake, which is why this sat EXPERIMENTAL with the note 'no real Windows audio device has ever been routed'. aletheia/audio_windows.py is the real backend: one PortAudio stream pair per approved route, frames pumped through a bounded ring, nothing written to disk and no buffer outliving the session. A route counts as active only when FRAMES HAVE MOVED since the last observation, so a stream that opened and then went silent - a device another app took, a headset that wandered off - reads as dead on the next look instead of as a working bridge. No virtual audio cable is required: the Bluetooth Hands-Free endpoints Windows already publishes are ordinary devices, so there is no kernel driver to install.",
      "module": "aletheia.audio_windows"
    },
    {
      "id": "core.autostart",
      "description": "Register and audit the Windows tasks that make Aletheia permanent: logon trigger plus a repeating watchdog",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "python -m aletheia.autostart doctor|install|show; python -m aletheia.supervisor install|status",
      "module": "aletheia.autostart",
      "notes": "Built 2026-08-27 after a live outage: the Core aborted at 07:42 and, because the task had only an at-logon trigger and the operator never logged off, nothing was listening for six hours. audit() holds the always-on contract as a pure function (repeating, unbounded, power-blind, restarting, IgnoreNew) so a broken registration is a finding, not a surprise.",
      "verification": "Proven live on the PC 2026-08-27: both processes killed at 14:15:47Z, Core answering again at 14:15:59Z (12s) and the room voice at 14:16:05Z, with no human action."
    },
    {
      "id": "core.liveness",
      "description": "Heartbeat while up, and a measured outage duration on the next start - journaled and emitted on the bus",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.core.core_tick beats every sync beat; core.main calls note_start; GET /api/status carries it; supervisor status prints it",
      "module": "aletheia.liveness",
      "notes": "The 2026-08-27 outage was undetected, not merely unrepaired. A gap longer than OUTAGE_AFTER_S becomes a core.outage_ended event, so the same watcher/proactive path that handles mail handles absence."
    },
    {
      "id": "reason.infer",
      "description": "Bounded, tool-less reasoning on the operator's own subscription: proposals only, never actions",
      "provider": "claude.cli",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.planner.compile; aletheia.reasoner CLI adapter",
      "module": "aletheia.reasoner",
      "notes": "The first provider that actually satisfies brain.contract - before this, brain.deterministic answered every input with 'clarify', which is why the 27-slot grammar was the whole of what could be expressed. Degrades honestly to that same fallback when the CLI is absent, and the reason reaches the operator.",
      "verification": "tests/test_reasoner.py asserts no tools, no API key flag, a timeout and a neutral cwd on every invocation"
    },
    {
      "id": "intent.plan",
      "description": "Compile an arbitrary sentence into a plan of validated commands, named capability gaps and manual steps",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_always",
      "caller": "intercom kind 'intent' (voice, Command Center, ChatGPT relay); python -m aletheia.planner; python -m aletheia.intents new",
      "module": "aletheia.planner",
      "notes": "A model proposes; the registries and gates dispose. Every proposed step goes through intercom.validate_kind_args, every named capability through the live registry, and a gap claimed on an AVAILABLE capability is refused rather than reported as a limitation."
    },
    {
      "id": "intent.execute",
      "description": "Run an approved plan later, bound to a sha256 of exactly the plan that was approved",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "aletheia.runtime.tick each Core beat (_run_approved_intents); python -m aletheia.intents run",
      "module": "aletheia.intents",
      "notes": "Same binding ratified for computer control and email. A plan edited between the ask and the approval no longer matches its hash and is refused, never adapted. Halt is re-read before every step.",
      "verification": "tests/test_intents.py: tampered plans refused, denied plans retired"
    },
    {
      "id": "errand.run",
      "description": "One approval-bound web errand - buy, book, cancel, submit - executed through the browser gate and stopped at §143 boundaries",
      "provider": "aletheia.local",
      "status": "EXPERIMENTAL",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.errands; aletheia.runtime.tick each Core beat (_run_authorized_errands)",
      "module": "aletheia.errands",
      "notes": "aletheia.errands: one approval-bound, evidence-verified web errand executed through the existing browser gate (browse.interact refuses before the browser opens unless the approval is APPROVED). Authorization binds to a sha256 of {site, kind, steps, ceiling}; a step or a ceiling edited afterwards is refused, not adapted. §143 boundaries - bank step-up, one-time codes, CAPTCHA, signatures, ID checks, biometrics, consent - stop the errand and hand the operator the remainder. This is the primitive §152 asks for: purchase.execute, reservation.book and subscription.cancel are the same errand with different kinds, and the next class of task is too."
    },
    {
      "id": "finance.hand_off",
      "description": "Carry a money movement to the §143 boundary and stop: everything prepared, the authorization left to the operator",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "low",
      "approval_policy": "none",
      "caller": "python -m aletheia.assistant; aletheia.finance.hand_off / handoffs / settle",
      "module": "aletheia.finance",
      "notes": "No credential is accepted or stored and nothing is sent. Preparing a payment he will authorize himself is low-risk precisely because it cannot move money; the risk lives in finance.transact, which stays NOT_BUILT on purpose."
    },
    {
      "id": "access.remote",
      "description": "Authenticated, TLS-only access to the Core from off this machine: scoped bearer tokens, rate limited, journaled",
      "provider": "aletheia.local",
      "status": "NEEDS_CONFIGURATION",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.access mint|list|revoke; aletheia.core.Handler.authorized() gates every remote request; aletheia.core.make_server refuses the bind without both conditions",
      "module": "aletheia.access",
      "notes": "The transport §92 called a ticket. Off-loopback listening needs BOTH a minted token and a TLS certificate, and refuses without either - port 8777 is never simply exposed. Tokens are stored as sha256 only, expire, carry a scope (read answers GET and nothing else), and lock an address out after 5 wrong guesses. Loopback is deliberately unchanged and unauthenticated: same trust boundary the Core has always had. NEEDS_CONFIGURATION because the operator has minted no token and supplied no certificate - `tailscale cert` is the recommended route, since it also solves reaching home without opening a port.",
      "verification": "tests/test_access.py: 29 tests, most asserting something does NOT happen - no token means no bind, no TLS means no bind, a read token cannot POST, a revoked token dies without a restart"
    },
    {
      "id": "policy.delegate.consume",
      "description": "Spend a standing grant so an approval he already gave is not asked again - never for high-risk or operator_always",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "aletheia.policy.request(..., capability=...) - every approval site in the repo: director, errands, intents, mail, calendar_provider, audio_router",
      "module": "aletheia.authority",
      "notes": "authority.py could record delegated authority since the systems layer and nothing consumed it; every approval still went to him. satisfy() is the consumer, wired into the ONE place approvals are created. allows() re-reads the registry at spend time, so a grant file edited on disk to name a high-risk capability still buys nothing: §56 L4 holds structurally, not by remembering.",
      "verification": "tests/test_authority_consumption.py: a forged grant over email.send is refused; exhausted, revoked, expired and corrupt grants all authorize nothing; a broken grant store fails closed"
    },
    {
      "id": "context.situational",
      "description": "A bounded, private snapshot of NOW - calendar, tasks, waiting replies, room, referents - assembled for the reasoning layer",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "read",
      "approval_policy": "none",
      "caller": "aletheia.planner.compile on the production path (never when a caller supplies its own provider or context); aletheia.advisor",
      "module": "aletheia.situational",
      "notes": "What makes 'move my next meeting' and 'handle that' resolvable against real state instead of guessed. Minimized on purpose: notification and email BODIES are omitted, room observed_state is allow-listed to simple fields, every string and collection is bounded, and byte pressure drops whole least-recent records rather than slicing JSON mid-object. In memory only - nothing here is persisted or committed. It is context, never authority: the planner and advisor prompts both declare it UNTRUSTED DATA, because a calendar title or task text can contain instruction-like wording.",
      "verification": "tests/test_situational.py; tests/test_planner_situational_context.py"
    },
    {
      "id": "advisor.triage",
      "description": "Proactive judgment on one observed event: ignore, notify, or suggest - never act",
      "provider": "claude.cli",
      "status": "NEEDS_CONFIGURATION",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "aletheia.runtime.process_new_events each Core beat (_advisor_judgment); python -m aletheia.advisor status|configure",
      "module": "aletheia.advisor",
      "notes": "ChatGPT PR (situational-context-v1), Claude-reviewed 2026-08-27. DISABLED by default and NEEDS_CONFIGURATION until the operator opts in at ~/.aletheia/advisor.json - building a proactive brain is not permission to send every event through a model. Bounded when on: an event-kind allowlist, a per-kind cooldown, hourly notification and daily suggestion caps, a confidence floor that demotes weak suggestions to IGNORE, halt-aware, and one exclusive receipt per event so an event is judged once. A SUGGEST publishes a notification and a recent referent - it is NOT an Intent and NOT an approval, so the operator must still ask, and that request is compiled and gated through the ordinary planner (§70).",
      "verification": "tests/test_advisor.py: unknown output fields refused, IGNORE/NOTIFY may not carry a suggestion, rate limits and the confidence floor demote rather than publish"
    },
    {
      "id": "calendar.authorize",
      "description": "One-time official OAuth consent for a live calendar account, on this machine, through the system browser",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "python -m aletheia.calendar_auth google|microsoft",
      "module": "aletheia.calendar_auth",
      "notes": "aletheia/calendar_live.py picks ONE source of truth - an official OAuth provider when configured, the secret-ICS fallback otherwise - because mirroring both against the same account would double-count busy time. Consent is a one-time local desktop flow (aletheia/calendar_auth.py): system browser, PKCE-S256, high-entropy state compared in constant time, one loopback callback on an ephemeral port. No code, access token, refresh token or client secret is printed, journaled or committed; the refresh config lives in ~/.aletheia/calendar-live.json and rotated tokens in gitignored state/private/calendar/oauth. The loopback listener accepts exactly one callback, refuses duplicated or mixed code/error parameters, suppresses HTTP request logging because authorization codes ride in the URL, and sets Referrer-Policy: no-referrer so the code cannot leak onward. Existing configuration is never overwritten without --replace."
    },
    {
      "id": "meeting.negotiate",
      "description": "Arrange a meeting with a person across days: offer times, wait for the reply, book the slot they chose",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_always",
      "caller": "python -m aletheia.scheduling new|reply|list|abandon; aletheia.runtime.tick each Core beat (_reconcile_scheduling, and _scheduling_reply routes correlated mail.reply events)",
      "module": "aletheia.scheduling",
      "notes": "Phase 15, the playbook's first big orchestration test. Every part existed and none were joined: contacts resolved a person, calendar found slots, mail drafted and gated, the bus waited for a reply, and meetings.propose did the first two and stopped. This is the state machine that survives Tuesday (§27) - OFFERING, SENT, INTERPRETING, BOOKING, BOOKED, NEEDS_OPERATOR, ABANDONED - reconciled each beat so it keeps its place across restarts and a human who answers when they feel like it. It SEQUENCES approvals and never substitutes for one: sending is email.send (operator_always), booking is calendar.write (operator_always, hash-bound to the exact event). A reply is interpreted by a tool-less provider against the exact slots offered, and anything below 0.75 confidence - or a counter-proposal, a decline, or an unreadable answer - becomes NEEDS_OPERATOR, because booking the wrong hour of someone else's week is not recoverable.",
      "verification": "tests/test_scheduling.py (25): an APPROVED send approval does not count as sent - only the .sent.json delivery marker does; a write that returns no event id is NOT booked; out-of-range or unaccompanied slot indices from the model are refused"
    },
    {
      "id": "email.read_body",
      "description": "Read the body text of a received email, not just its headers",
      "provider": "aletheia.local",
      "status": "NOT_BUILT",
      "risk_class": "high",
      "approval_policy": "operator_always",
      "caller": "TICKET: no caller. Wanted by aletheia.scheduling.note_reply, which can see that a meeting reply arrived but not what it says.",
      "notes": "Named rather than worked around (§105). aletheia.mail deliberately fetches BODY.PEEK[HEADER] - headers only - so Aletheia never holds the operator's correspondence, and the private stores keep fingerprints rather than text. That is the right default, and it is exactly why a meeting negotiation cannot read 'Tuesday works' for itself; it asks the operator to relay the answer in one line instead of guessing an acceptance from a subject. Building this widens what Aletheia reads of his mail and is a privacy decision that is his to make, not a missing function: it needs an explicit ruling on which messages may be read, how long any text is retained, and whether a body may ever reach a reasoning provider."
    },
    {
      "id": "perception.screen",
      "description": "Read what is on the operator's screen from the accessibility tree and answer a question about it",
      "provider": "aletheia.local",
      "status": "AVAILABLE",
      "risk_class": "medium",
      "approval_policy": "operator_once",
      "caller": "voice: 'what's on my screen', 'what am I looking at', 'what does this say'; intercom kind screen_ask; python -m aletheia.perception look|ask",
      "module": "aletheia.perception",
      "notes": "Playbook §86, done §13's way: accessibility tree before screen coordinates before vision-only pixels. That is not a compromise - a UIA tree knows a control is a button called 'Submit', where a screenshot knows it is beige - and it needs no vision model and no API key. Two boundaries make it separate from computer.control rather than a flag on it. LOOKING IS NOT PRESSING: this module can only emit list_windows and inspect_controls, so invoke/set_text/open_app/close_window are absent by construction, not refused by a check (the browse.read_page vs browse.interact precedent, §61). A SCREEN IS NOT SAFE TEXT: password fields are dropped rather than masked, credential-shaped values are replaced wherever they appear, and the observation is bounded before any provider sees it - and the model's own answer is redacted again on the way back. The observation is declared UNTRUSTED DATA, because a window title is attacker-chosen in exactly the way an email subject is.",
      "verification": "19 tests: acting steps refused with nothing performed, password contents never present in the observation, oversized screens trimmed by whole records. Live on the PC 2026-08-27: asked what was open and answered correctly from real windows."
    }
  ]
}
